name: be_my_eye
description: Be My Eye mobile client
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.2.2
  http: ^1.2.2
  just_audio: ^0.10.6
  path_provider: ^2.1.6
  record: ^7.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
